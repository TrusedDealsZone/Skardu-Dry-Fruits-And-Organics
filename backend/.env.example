# Backend Environment Configuration
PORT=5000
NODE_ENV=production

# MongoDB Atlas Connection String
# Get this from MongoDB Atlas -> Connect -> Connect your application:
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/skardu_dryfruits?retryWrites=true&w=majority

# JWT Authentication Secret
JWT_SECRET=your-secure-jwt-secret-key-production-2026

# Allowed CORS Origins (Comma separated)
CLIENT_URL=http://localhost:5173,https://your-app.vercel.app,https://yourdomain.com
